import SettingsClient from '@/components/features/settings/SettingsClient'

export const metadata = { title: "Mon compte — L'Atelier Mobile" }

export default function SettingsPage() {
  return <SettingsClient />
}
